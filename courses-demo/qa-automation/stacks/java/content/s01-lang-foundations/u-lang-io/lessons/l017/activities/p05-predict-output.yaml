id: p05
kind: predict_output
phase: practice
code: |
  // псевдо: mapper настроен стандартно, record User(String name) {}
  // JSON: {"name":"Ann"}
  // User u = mapper.readValue(json, User.class);
  // System.out.println(u.name());
question: Какой результат ожидаем при корректном mapping?
choices:
  - id: a
    text: Ann
    correct: true
  - id: b
    text: 'null'
  - id: c
    text: name
  - id: d
    text: '{}'
explain: |
  Поле name совпадает с JSON key.
