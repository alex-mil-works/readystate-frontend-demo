# Логи (logs)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Зависимости (dependencies)
node_modules

# Сборка / генерация (build / generated)
dist
dist-ssr
dist/generated-content
*.local

# Developer scratch (not part of the product)
.local/

# Local curriculum (authoring + compiled bundles — do not commit)
.courses
src/shared/content/generated/**/*.json

# Артефакты тестов и coverage
coverage
playwright-report
playwright/.cache
test-results
*.tsbuildinfo

# Переменные окружения (env) — секреты не коммитить
.env
.env.*
!.env.example

# Редактор / ОС (editor / OS)
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Кэш Vite
.vite

# Vercel CLI link (local only; CI writes from secrets)
.vercel

# Yarn Berry (локальный install state; node_modules-linker)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Прочие кэши
.cache
