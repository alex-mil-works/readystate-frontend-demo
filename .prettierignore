# Сборка и зависимости (build / dependencies)
dist
node_modules
coverage
playwright-report
test-results
.vite

# Lockfile'ы — не форматировать Prettier'ом
yarn.lock
package-lock.json
pnpm-lock.yaml

# Developer scratch (not part of the product)
.local

# Сгенерированный контент (generated content)
dist/generated-content
